# Alpine Team Manager by TechBit v0.3.1

## 変更点
- 選手登録は「SAJ競技者番号 → 選手登録」のワンタッチ方式
- 氏名、所属、Birth、SL/GS/SG/DH/SCポイントをSAJ公開ポイントリストから取得
- 登録済み選手の一括SAJ更新
- GitHub PagesからのCORS回避用 Cloudflare Worker を同梱

## 1. Cloudflare Worker
このフォルダで:

```bash
npx wrangler deploy
```

表示された `https://snowtech-saj-api.xxxxx.workers.dev` をコピー。

## 2. GitHub Pages
`index.html`, `manifest.webmanifest`, `sw.js`, `icon.svg` をGitHub Pagesのリポジトリ直下に配置。

## 3. アプリ設定
SnowTech → 管理 → `SAJ連携API URL` にWorker URLを入力 → 保存。

以後:
`選手 → 選手追加 → SAJ競技者番号 → 選手登録`

## 注意
- SAJの公開ページのみ参照します。
- SAJサイトのHTML構造変更時はWorker側のパーサ修正が必要になる場合があります。
- 学年はBirthだけで確定せず、必要ならアプリ上で手動補正します。


## v0.3.1 SAJ年度検索
- SAJ競技者番号は年度共通として扱います。
- 当年度の最新発行済みポイントリストを優先検索。
- 当年度に見つからなければ前年度を自動検索。
- 2026年9月時点では 2026/2027 (season_code=2027) → 2025/2026 (season_code=2026) の順です。
- APIレスポンスに seasonCode / seasonLabel を追加しています。


## v0.3.2
- SAJ選手登録をポイントリスト依存から変更
- `/alpine/biography/{SAJ番号}` を本人情報の主データ源に変更
- ポイントリスト未掲載でも選手登録可能
- 当年度→前年度の公式ポイントリストを補助参照
- Biographyの過去大会履歴も選手詳細へ保存
- テスト基準: 03028493


## v0.3.3
- SAJ連携APIを `https://snowtech-saj-api.take6583.workers.dev` にデフォルト固定
- 旧バージョンのLocalStorageが残っていてAPI URLが空でも自動的にデフォルトAPIを使用


## v0.3.4
- SAJ biographyのプロフィール解析を実HTML構造に合わせて修正
- 氏名・ローマ字名・生年月日・性別・所属をフラットテキストから取得
- テスト番号: 03028493

## v0.3.5
- SAJ biographyプロフィール解析を再修正
- 正規表現の不要なエスケープを除去
- SAJ公開ページの実際の表示順
  `SAJ番号 → FIS番号 → 氏名 → ローマ字名 → 生年月日/性別 → 所属`
  に合わせて解析
- 03028493 を基準にプロフィール取得を修正

## v0.3.6
- SAJポイント取得を強化
- 当年度 → 前年度の順で検索
- ポイントリスト画面に加え、発行カレンダー上のCSV/TXT等ダウンロードファイルも検索
- SAJ競技者番号をダウンロードデータ内から直接検索
- `/api/debug-points?saj=XXXXXXXX` を追加
- デバッグAPIでカレンダーURL、リンク数、候補ダウンロードURL、選手番号一致状況、解決結果を確認可能

## v0.3.7
- SAJ取得データの生年月日・性別を選手データへ保存
- 既存選手も「登録選手をSAJ更新」で生年月日・性別を更新
- 選手編集画面に生年月日・性別を追加
- 所属選手画面を「男子」「女子」に分離
- 性別未取得の旧データは別枠に表示し、SAJ更新を案内
- 選手一覧に生年月日を表示
- 選手詳細に性別・生年月日を表示

## v0.3.8
- チームランキングを男子／女子に分離
- 大会ページに「公認大会情報取得」を追加
- Cloudflare Workerに `/api/saj-competitions?season=YYYY` を追加
- SAJアルペン大会カレンダーから大会名・日程・開催地・種目・大会URLを取得
- 参加希望大会をチェックしてSnowTech大会管理へ一括登録
- 取り込んだ大会は年間予定へ自動反映
- SAJ大会キーで二重登録を防止

## v0.3.9
- SAJ大会カレンダー取得方式を修正
- 推測したクエリパラメータを廃止
- SAJ大会カレンダーHTMLの検索フォームを解析し、実際のselect name/valueを利用
- GET/POSTのフォームmethod/actionにも追従
- `/api/debug-competition-calendar` を追加
- デバッグAPIでフォームmethod/action/select名/シーズンoptionを確認可能

## v0.4.0
- SAJ公認大会取得の0件問題を修正
- SAJ `/search_competitions` はシーズンだけでなく競技選択が必須
- 検索フォームから「アルペン / Alpine」の実際のoption valueを自動検出
- シーズン + アルペンを必ずセットして検索
- フォームactionで0件の場合は `/search_competitions` へ同じ実フィールドを直接GET
- debug APIに detectedSeasonField / detectedAlpineField を追加

## v0.4.1
- SAJ大会リンクの2形式に対応
  - /alpine/YYYY/competition/ID
  - /alpine/competition/ID
- 大会リンクがtable row外にある場合のfallback追加
- 大会名がリンク文字列ではなく通常セルの場合の推定を追加
- 0件時に diagnostic をAPIレスポンスへ付加
  - htmlLength
  - hasNoSchedule
  - hasCompetitionText
  - competitionLinkCount
  - sampleCompetitionLinks
  - visibleExcerpt

## v0.4.2
- ヘッダー右上にバージョン `v0.4.2` を常時表示
- SAJ公認大会画面に地域単位フィルターを追加
- 地域: 北海道 / 東北 / 関東 / 甲信越 / 北陸 / 東海 / 近畿 / 中国 / 四国 / 九州・沖縄
- 初期状態は全地域ON
- チェック状態はLocalStorageへ保存
- 全地域ON / 全地域OFFボタン追加
- 全国取得データは保持したまま表示だけ地域フィルター
- 開催県を判定できない大会は誤除外を避けるため表示

## v0.4.3
- SAJ大会カレンダーの「月」を必須検索条件として扱う
- liveフォームから月selectを自動検出
- 1月〜12月の実option値をWorker内部で順番に検索
- 月ごとの大会結果を統合し、大会キーで重複除去
- ユーザー側は従来どおり1回の「公認大会情報取得」で全国一覧を取得
- 地域フィルターは取得後の表示絞り込みとして維持
- 0件時diagnosticに月別検索結果を追加
- 成功時monthSummaryをAPIレスポンスへ付加

## v0.4.4
- SAJ大会検索フォームをfield nameで厳密に特定
- `/search_competitions` を大会取得の主経路へ変更
- `search_prefecture=すべて` を送信しない
- 開催地未指定を「全国」として検索
- 月別巡回は維持
- diagnosticsにvalidation/noScheduleを追加
- ヘッダー表示 v0.4.4

## v0.4.5
- SAJ開催地未指定では全国検索にならない挙動へ対応
- Worker APIを1か月単位に変更: `/api/saj-competitions?season=2026&month=2`
- 1回のWorker内で47都道府県を実際のselect option値で巡回
- SnowTech画面側が1〜12月を順番に取得し全国大会を統合
- 大会キーで重複除去
- 取得途中も部分結果を画面表示
- 地域ON/OFFフィルターは従来どおり取得後に適用
- `/search_competitions` から大会カレンダーへリダイレクトされる挙動を正常扱い

## v0.4.6
- Cloudflare Workers Free の外部subrequest上限 50/request 対応
- 47都道府県を3バッチ（16/16/15）に分割
- API: `/api/saj-competitions?season=2026&month=2&batch=1`
- 1バッチ最大約33 subrequests（base取得1 + 16県×redirect込み2）
- SnowTech側は12か月×3バッチ=36回を自動実行
- 部分取得結果を随時一覧へ表示
- batchInfo diagnostic追加
- ヘッダー v0.4.6

## v0.4.7
- SAJ大会検索GETで空文字パラメータを削除しないよう修正
- `search_discipline=` を明示送信
- SAJフォームの実際の検索URL構造に合わせる
- 47都道府県3バッチ方式は維持
- ヘッダー v0.4.7

## v0.4.8
- SAJ `/search_competitions` のセッションCookie引継ぎに対応
- HTTP 301/302/303/307/308をWorker側で手動追跡
- `Set-Cookie` をCookie jarへ保持し、リダイレクト先へ `Cookie` として送信
- ブラウザで検索する時と同じセッション状態を再現
- `search_discipline=` 空文字送信を維持
- 2月・山形県を診断プローブとして常時 `months` に記録
- 診断に redirectCount / sessionCookieCount を追加（Cookie値は出力しない）
- 47都道府県3バッチ方式を維持
- ヘッダー v0.4.8

## v0.4.9
- SAJ大会検索の競技を常にアルペンへ固定
- `search_sports_code=AL` を各検索リクエストで明示的に上書き
- `search_discipline=` は空欄のまま（アルペン全種目取得）
- 診断結果に `sportsCode:"AL"` を追加
- ヘッダー v0.4.9

## v0.5.0
- 診断用 `/api/debug-competition-js` を追加
- 大会カレンダーHTML内の script src / $.ajax / $.get / $.post / fetch / url: を抽出
- SAJカレンダー内部の大会データ取得先を特定するための診断
- `search_sports_code=AL` 強制指定は維持
- 既存の47都道府県3バッチ方式は維持
- ヘッダー v0.5.0

## v0.5.1
- SAJ大会取得をHTMLパース方式から内部JSON API方式へ全面変更
- SAJカレンダー自身が使用する `/api/search_competitions` をGET
- `sports_code=AL` を常時固定
- `prefecture=` で全国、`month=1..12` を月別取得
- `data.competitions` をSnowTech大会形式へ変換
- racesからGS/SL等の種目を集約
- Cookie / HTMLリダイレクト / 47都道府県3バッチ処理を大会取得本線から廃止
- 診断 `/api/debug-competition-api?season=2026&month=2` を追加
- ヘッダー v0.5.1

## v0.5.2
- v0.5.1生成時に残っていた旧v0.4系大会取得ブロックを完全削除
- `Expected "}" but found "if"` のWorkerビルドエラーを修正
- `/api/saj-competitions` はSAJ内部JSON API `/api/search_competitions` 方式のみを使用
- `sports_code=AL`（アルペン）固定
- worker.js / index.html JavaScript 構文チェック済み

## v0.5.3
- SAJ公認大会一覧の大会名をクリック可能に変更
- クリックするとSAJ競技データバンクの実際の大会情報ページを新しいタブで開く
- `target="_blank"` + `rel="noopener noreferrer"`
- URL未取得時は通常テキスト表示
- ヘッダー v0.5.3

## v0.5.4
- 年間予定表をA4横1枚向けの11月〜4月カレンダー形式へ追加
- 縦軸=日付、横軸=月（11月 / 12月 / 1月 / 2月 / 3月 / 4月）
- 各セルに `1日(月)` と当日の予定を表示
- 予定一覧に加え、年間予定表プレビューを画面表示
- 表示シーズン選択を追加（YYYY/YYYY+1）
- 印刷時は年間予定一覧を非表示にし、A4横の年間予定表を優先表示
- ボタン名を `A4年間予定表PDF` へ変更
- ヘッダー v0.5.4

## v0.5.5
- シーズンカレンダー最左列の日付列を削除
- 各月ヘッダーを `11月` / `12月` / `1月` ... のみに変更
- 画面上で日曜・祝日を赤、祝日でない土曜を青表示
- 2026/2027の祝日を内閣府公表日程に合わせて反映
- PDF印刷時は色を使わず、白背景・黒文字・罫線のみ
- SAJ大会一覧の大会名リンクを白文字固定、visited/hover/activeでも色変更なし
- 地域案内文を `大会出場するエリアにチェックを入れてください。` に変更
- ヘッダー v0.5.5

## v0.5.6
- PDF印刷時も曜日色を維持
- 日曜・祝日 = 赤文字
- 祝日ではない土曜 = 青文字
- 背景色は使用せず白背景
- 予定本文は黒文字
- 罫線 + 文字中心のA4横印刷仕様を維持
- ヘッダー v0.5.6

## v0.5.7
- HOME画面を削除
- 起動時の初期画面を年間予定へ変更
- 年間活動スケジュールは年間予定表のみ表示
- 年間予定画面の操作は `予定追加` / `年間予定PDF` のみ
- 活動種別フィルターを削除
- 通常の縦型予定一覧を削除
- 大会管理のチェック項目（申込 / 参加費 / 宿泊 / 交通 / 保護者 / 選手確認）を削除
- 大会カード上のチェック表示も削除
- 既存保存データ内の checks は読み捨て互換（データ移行不要）
- ヘッダー v0.5.7

## v0.5.8
- 年間予定表をA4縦2枚構成へ変更
- 1ページ目：11月 / 12月 / 1月
- 2ページ目：2月 / 3月 / 4月
- 各予定に `引率：○○` を表示
- 手動予定は担当コーチ欄を引率者名として利用
- 大会は大会管理で設定した引率コーチを表示
- PDF印刷はA4 portrait、ページ間で改ページ
- 日曜・祝日=赤、土曜=青をPDFでも維持
- ヘッダー v0.5.8

## v0.5.9
- 年間予定表の画面表示は11月〜4月の6か月横並びへ変更
- 画面は横スクロールで閲覧
- PDFだけA4縦2枚（11〜1月 / 2〜4月）で印刷
- 引率者表示を画面/PDF双方で維持
- 所属選手のSAJデータを端末起動時に1日1回自動更新
- 同日中の再起動・再読込では自動更新しない
- 選手一覧にSAJ取得日を表示
- 選手一覧にポイントリストNo.を表示
- 選手詳細にも取得日 / ポイントリストNo. / シーズンを表示
- WorkerでSAJポイントリスト発行カレンダーのリストNo.を取得元ポイントに紐づけ
- ヘッダー v0.5.9

## v0.6.0
- スマートフォンPWAで更新が反映されない問題への対策
- アプリ起動時に `snowtech-alpine-*` の旧Cache Storageを削除
- LocalStorageのチーム/選手/予定データは削除しない
- Service Worker登録時に `updateViaCache: 'none'`
- `sw.js?ver=060` でService Worker自体のキャッシュ回避
- 起動時に `registration.update()` を実行
- 新Service Workerは `skipWaiting()` + `clients.claim()` で即時切替
- ヘッダー v0.6.0

## v0.6.1
- 年間予定表をタップすると全画面表示
- 全画面右下に固定「戻る」ボタン
- 全画面カレンダーのみ2本指ピンチで70〜200%拡大縮小
- 通常UIは viewport の user-scalable=no / maximum-scale=1 でブラウザ拡大縮小をロック
- 全画面カレンダーではブラウザズームではなくアプリ独自ズームを使用
- 横スクロールは通常画面/全画面とも維持
- PDF仕様への影響なし
- ヘッダー v0.6.1

## v0.6.2
- 大会管理の表示地域は全地域OFFを初期値に変更（地域設定キーをv2へ更新）
- 大会出場する地域だけチェックして表示
- SAJ大会の開催地表示を都道府県名のみへ変更（例: 長野県 上田市 → 長野県）
- 選択大会反映後に `年間予定に反映いたしました` を表示
- 登録済み大会の画面表示を `開催日 / 大会名 / 削除` のみに簡略化
- 登録済み大会削除時は年間カレンダーからも即時削除
- 大会スケジュールPDFでは従来の詳細（場所 / 種目 / 締切 / 出場 / 引率 / 備考）を維持
- 大会管理ボタンを `公認大会情報取得 / その他大会追加 / 大会スケジュールPDF` に変更
- `その他大会追加` は同じボタンで開閉トグル
- その他大会追加フォーム下部ボタンは `保存` のみに変更
- ヘッダー v0.6.2

## v0.6.3
- チームランキングにSAJ登録番号を表示
- その他大会追加フォームから参加選手欄を削除
- その他大会追加フォームから引率コーチ欄を削除
- その他大会追加フォームから備考欄を削除
- その他大会は内部互換用として athleteIds / coachIds を空配列、noteを空文字で保存
- 管理画面からSAJ連携API URL表示を削除
- SAJ Worker URLは内部固定設定として利用
- チーム設定保存はチーム名のみ
- ヘッダー v0.6.3

## v0.6.4
- 年間予定PDFの1ページ目/2ページ目で「1日」の開始位置を同一高さに調整
- 両ページの印刷ヘッダー領域を固定高さに統一
- 印刷時タイトルを `チーム名 年間予定表` に変更
- シーズン/対象月は固定高さのサブタイトルへ分離
- 公認大会情報取得ボタンは初期状態で青くしない
- 公認大会情報取得 / その他大会追加 を排他的な切替ボタン化
- 公認大会表示中にその他大会追加を押すと、公認大会パネルを閉じてその他大会フォームへ切替
- 選択中のボタンだけ青表示
- 同じ選択ボタンを再度押すと閉じて通常状態へ戻る
- ヘッダー v0.6.4

## v0.6.5
- チームランキングの氏名右側に生年月日を表示
- SL / GS / SG のレースポイントを太字表示
- 男女ランキング共通仕様
- ヘッダー v0.6.5

## v0.6.6
- スマートフォンの所属選手一覧を横スクロール対応
- 選手テーブルに最低横幅を設定し、列を潰さず表示
- iPhone / Androidで慣性スクロール対応
- PC表示および印刷仕様への影響なし
- ヘッダー v0.6.6

## v0.6.7
- スマートフォンの所属選手一覧横スクロールを実表に正しく適用
- 男子 / 女子 / 性別未取得の3表を横スクロールコンテナでラップ
- 各列の最低幅を設定し、文字や数値の潰れ・不自然な折返しを防止
- SAJ No. / SL / GS / SG / 取得日 / PL No. / 操作まで横スワイプで閲覧
- スマホ時のみ `← 横にスワイプして全項目を表示 →` を表示
- PC表示・印刷仕様への影響なし
- ヘッダー v0.6.7

## v0.6.8
- 昨シーズン / 今シーズン / 来シーズンを常に管理可能に変更
- 年間予定のシーズン選択に常時3シーズンを表示
- 2026年9月時点では 2025/2026（昨）・2026/2027（今）・2027/2028（来）
- スキーシーズン切替基準を11月から7月へ変更しSAJシーズンと統一
- 過去/未来に予定データがある追加シーズンも選択肢から消さない
- SAJ公認大会取得のシーズン選択も 昨 / 今 / 来 の順に統一
- ヘッダー v0.6.8

## v0.6.9
- 年間予定の表示シーズンに `表示シーズンをロック` チェックを追加
- ロック中はシーズンプルダウンを無効化し誤操作を防止
- 選択中シーズンとロック状態をLocalStorageへ保存
- アプリ再起動後もロックしたシーズンを維持
- ロック解除後は昨 / 今 / 来シーズンを再選択可能
- ヘッダー v0.6.9

## v0.7.0
- 所属選手一覧を男子 / 女子で分けず1つの表に統合
- 性別は一覧内の列として表示
- スマートフォンでは統合表全体を横スクロール
- 氏名 / 性別 / 生年月日 / 所属 / SAJ No. / SL / GS / SG / 取得日 / PL No. / 操作を1表で確認
- 表示順は 男 → 女 → 性別未取得、同区分内は氏名順
- ヘッダー v0.7.0

## v0.7.1
- 所属選手一覧の並び順を変更
- 性別区分は 男 → 女 → 性別未取得
- 同じ区分内は生年月日の古い順（年上から順）
- 生年月日未取得は同区分の最後
- 生年月日が同じ場合は氏名順
- ヘッダー v0.7.1

## v0.7.2
- 年間予定表の日付セルをタップして一言の簡易予定を直接入力可能
- 例: `雪上練習　雫石`
- 簡易入力は日付を自動設定し、そのまま年間予定へ保存
- 簡易予定は種別名を付けず入力文字列をそのままカレンダー表示
- 通常画面 / 全画面カレンダーの両方でセルタップ入力に対応
- 日付セル以外をタップした場合は従来どおり全画面表示
- 詳細な `予定追加` 機能は従来どおり利用可能
- PDFにも簡易予定を反映
- ヘッダー v0.7.2

## v0.7.3
- 選手タブ下部に残っていたランキング表示を削除
- ランキングを独立したランキングタブへ完全移動
- SAJ公認大会候補の大会名リンクを廃止し通常文字表示へ変更
- SAJ大会には青い `大会詳細` ボタンを追加
- `大会詳細` 押下でSAJの実大会情報ページを新しいタブで表示
- 登録済み大会一覧にもSAJ大会だけ `大会詳細` 青ボタンを表示
- その他大会には詳細ボタンを表示しない
- ヘッダー v0.7.3

## v0.7.4
- 所属選手名クリック時の詳細を専用画面表示へ変更
- 専用画面に `← 所属選手一覧へ戻る` を追加
- SAJ大会の詳細ボタンを大会名より左側へ配置
- ボタン名を `大会詳細` から `詳細` へ変更
- `詳細` ボタンをコンパクト化
- 大会期間を2行表示（開始日～ / 終了日）
- 公認大会候補一覧も同じレイアウトへ統一
- ヘッダー v0.7.4

## v0.7.5
- ナビゲーションを6ボタン・2段3列へ変更
  - 年間予定 / 大会 / 会場情報
  - 選手 / ランキング / チーム管理
- `管理` を `チーム管理` へ改名
- 新規 `会場情報` 画面を追加
- 全国主要スキー場を地域別に掲載
- 各スキー場に青い `公式` ボタンを配置
- 公式サイトは新しいタブで開く
- スマホは会場一覧を1列表示
- ヘッダー v0.7.5

## v0.7.6
- 所属選手のSAJ競技者番号入力欄と選手登録ボタンを常時表示
- `登録選手をSAJ更新` を `ポイント更新` に変更
- `SAJポイントリスト` を `ポイントリスト参照` に変更
- 年間予定の通常画面を閲覧専用に変更
- 通常画面の `予定追加` ボタンを撤去
- 通常カレンダーはセルを含め、タップで必ず全画面へ移行
- 簡易入力は全画面カレンダーのセルタップ時のみ有効
- 詳細な `予定追加` も全画面内のボタンからのみ利用可能
- 全画面を閉じると入力フォームも閉じる
- ヘッダー v0.7.6

## v0.7.7
- 起動時にSnowTechタイトル / ツール選択画面を追加
- `セッター用ツール` と `マネージメントツール` の2択
- セッター用ツールは `https://m6jm4s654p-lab.github.io/snowtech/` へ移動
- マネージメントツールは現在のAlpine Team Manager by TechBitへ移行
- 管理画面ヘッダーに `ツール選択` ボタンを追加
- ページを再読み込みするとタイトル選択画面から開始
- ヘッダー v0.7.7

### 同一GitHub Pagesへ完全統合する場合
現在 `https://m6jm4s654p-lab.github.io/snowtech/` は既存セッター用ツールのルートです。
同じURLの下で両ツールを共存させる場合は、既存セッター用ツールを `setter/` 等のサブフォルダへ移し、
タイトル画面のセッターリンクを `./setter/` に変更してください。

## v0.7.8
- 年間予定の通常横スクロール画面でセルタップ編集を再有効化
- 横ドラッグ時はスクロール、タップ時のみセル編集
- 年間予定上部に青い `全画面表示` ボタンを新設
- 通常画面のセルタップでは全画面へ移行しない
- 全画面モードでもセルタップで同じ簡易編集が可能
- 既存の簡易予定がある日は現在の文言を表示して上書き編集
- 通常予定がある日はその予定名を簡易編集
- 空セルは一言予定を新規登録
- SAJ公認大会など大会データはセル編集で変更しない
- 全画面モードの `予定追加` ボタンを削除
- ヘッダー v0.7.8

## v0.7.9
- 採用済みSNOWTECHデザインを正式ロゴとしてタイトル画面へ実装
- `SHIZUKUISHI IWATE / @snowtech` を含むロゴを使用
- ホーム画面追加用のPWAアイコンを新デザインへ変更
- 192px / 512px / Apple Touch 180px を追加
- manifestのアプリ名を `SNOWTECH` に統一
- faviconも新SNOWTECHアイコンへ変更
- Service Workerへ新ロゴ/アイコンをキャッシュ対象として追加
- ヘッダー v0.7.9

## v0.8.0
- 会場情報を一般的な有名スキー場一覧から、アルペン公認大会会場中心へ再構成
- SAJ/FIS公認大会の開催実績・予定が確認できる会場に `公認大会会場` バッジを表示
- 追加・強化した主な会場
  - カムイスキーリンクス / ぬかびら源泉郷 / サッポロテイネ
  - 岩手高原 / 雫石 / 安比高原 / 花輪
  - 赤倉温泉（山形） / 蔵王ライザ / 猪苗代 / フェアリーランドかねやま
  - スノーパーク尾瀬戸倉
  - 菅平高原パインビーク / 野沢温泉 / 志賀高原西舘山 / 戸隠 / ロッテアライ
  - あわすの / 白峰アルペン競技場
  - ほおのき平 / モンデウス
  - だいせんホワイトリゾート / 若桜氷ノ山
- 各会場の公式スキー場・施設サイトへのリンクを維持
- ヘッダー v0.8.0

## v0.8.1
- 会場情報を固定スキー場一覧から完全に変更
- 大会管理で実際に登録した大会会場だけを会場情報へ自動表示
- 年間予定の表示シーズンと会場情報を連動
- 来シーズンに別の大会会場を登録すれば、そのシーズンでは新しい会場が自動表示
- 同一会場で複数大会を登録した場合は1会場へまとめ、大会名を併記
- 既知会場は公式ホームページへの `公式` ボタンを自動表示
- 未登録の新会場も会場名自体は必ず表示し、公式URL未登録であることを表示
- SAJ大会新規取込時に都道府県情報も保存
- タイトル / ツール選択画面を白背景へ変更
- 白背景に合わせて選択カードを明るい配色へ変更
- ヘッダー v0.8.1

## v0.8.2
- 所属選手名タップ後の専用詳細画面は維持
- 大会履歴表示を修正前の形式へ復元
- 見出しを `過去大会成績` に変更
- 表示列を `日付 / 大会 / 種目 / 順位 / タイム / 獲得P` の6列へ復元
- 大会名は旧データ `race` と現行データ `title` の両方に対応
- スマホでは大会成績表も横スクロール可能
- 成績がない場合は `成績データなし` と表示
- ヘッダー v0.8.2

## v0.8.3
- 初期サンプルデータをすべて削除
- 新規利用時はチーム名 / コーチ / 選手 / 年間予定 / 大会を空の状態で開始
- チーム名の既定値 `ESS` を削除
- 新規初期化時のみ空データとなり、既存端末に保存済みのLocalStorageデータは維持
- 全データ削除後の再起動も空状態から開始
- ヘッダー v0.8.3

## v0.8.4
- ヘッダー右上の `ツール選択` ボタンを撤去
- `ツール選択` ボタンを6ボタンナビゲーション直下へ移動
- スマホでは中央配置し、押しやすいサイズへ変更
- タイトル画面の最下層背景をマネージメントツールと同じ濃色 `#06101a` に変更
- タイトルカード自体は白背景を維持し、SNOWTECHロゴの視認性を確保
- ヘッダー v0.8.4

## v0.8.5
- 印刷処理 `printSection()` の重複定義を解消
- 印刷ボタン1タップにつき `window.print()` を1回だけ実行
- 印刷中ロックを追加し、二重タップや重複呼び出しを防止
- 自動再試行・遅延実行を行わず、ユーザー操作から直接印刷を開始
- `afterprint` / `pageshow` で印刷ロックを解除
- ヘッダー v0.8.5

## v0.8.6
- 選手登録に `一覧から選択登録` を追加
- 性別（男/女）＋加盟団体（47都道府県）でSAJアルペンポイントリストを検索
- 検索結果をチェックボックスで複数選択し一括登録
- 一覧表示: 氏名 / Birth / SAJ No. / 加盟団体 / チーム名 / SL / GS / SG
- 登録済みSAJ No.は一覧で無効化し、重複登録を防止
- 一括登録時は各SAJ No.について既存 `/api/saj-athlete` を再取得し、生年月日・所属・最新ポイント・大会履歴を登録
- Workerに `/api/saj-athletes?sex=男&organization=岩手` を追加
- SAJ加盟団体の内部値は固定せず、ポイントリスト検索フォームのoptionから動的に解決
- 既存の競技者番号1名登録は維持

## v0.8.7
- `選択した選手を登録` の右側に `リストをクリア` ボタンを追加
- `リストをクリア` で取得済みのチェックボックス一覧を消去して閉じる
- 選択選手の一括登録が1名以上成功した場合、登録完了後にチェックボックス付き一覧を自動で閉じる
- 性別・加盟団体の選択欄はそのまま残し、必要に応じて再取得可能

## v0.8.9
- 公認大会情報一覧で大会を選択し「年間予定へ反映」した後、反映完了表示と同時に取得済み大会一覧を閉じるよう変更
- 年間予定への登録内容・重複防止処理は従来どおり

## v0.8.9
- ナビゲーションを8機能へ拡張し、PCは4列×2段、スマホは2列表示
- 名称変更: 大会管理 / 大会会場 / 選手管理 / チーム設定 / ツール切替
- `天気・積雪` を追加。登録済み大会会場を選択し、7日間の気温・降水確率・予想降雪・風速・最大瞬間風速を表示
- `チームボード` を追加。所属選手数、今後の大会数、次大会までの日数、直近予定・大会を集約
- 天気はOpen-Meteoを利用し、通信時のみ取得。既存のローカル保存機能は維持

## v0.9.0
- 大会管理の大会候補一覧上部にも「チェックした大会を年間予定へ反映」「閉じる」を追加
- 年間予定への反映成功後、大会候補一覧と公認大会情報取得パネルを自動で閉じる
- チームボードを削除
- ホーム画面を追加し、マネージメントツール起動時の初期画面をホームに変更
- ホームに年間予定 / 大会管理 / 大会会場 / 天気・積雪 / 選手管理 / ランキング / チーム設定 / ツール切替の8ボタンを配置
- ホームに本日を含む直近7日間の予定を表示
- 7日間予定には通常予定と登録大会を統合表示

## v0.9.1
- アプリ名称を `Alpine Team Manager by TechBit` へ変更
- タイトル画面の旧SNOWTECHロゴを、採用した透明背景の Alpine Team Manager / TechBit ロゴへ差し替え
- その他の画面は画像レイアウトを変更せず、表示上の名称のみ Alpine Team Manager 系へ変更
- 既存LocalStorageキーや内部互換識別子は維持し、保存済みチーム・選手・大会・予定データを引き継ぐ設計

## v0.9.2
- 年間活動スケジュールに「オフシーズン（5〜10月）も表示・印刷」チェックを追加
- OFF時は従来どおり11月〜翌4月、PDFはA4縦2枚
- ON時は5月〜翌4月の12か月を横スクロール表示
- ON時のPDF印刷対象は5〜7月 / 8〜10月 / 11〜1月 / 2〜4月のA4縦4枚
- オフシーズン設定はLocalStorageへ保存し、再起動後も維持
- 全画面表示も12か月幅に対応

## v0.9.3
- 天気・積雪の入力方式を変更。登録大会の会場を選択すると、都道府県または地域からスキー場候補を自動で絞り込み、一覧から選択できるように変更
- 手入力の「検索地名」を廃止
- 登録会場と公式会場名が一致する場合は候補を自動選択
- スキー場位置検索はOpenStreetMap Nominatimを優先し、Open-Meteo geocodingをフォールバックとして利用
- 年間予定PDF / 大会スケジュールPDFから `window.print()` を完全に廃止
- PDFボタンはブラウザ印刷ダイアログを開かず、A4 PDFをアプリ内で直接生成してダウンロード
- 年間予定PDFは従来の3か月/ページ構成を維持（通常2ページ、オフシーズンON時4ページ）
- 土曜青、日曜・祝日赤、チーム名・シーズン・引率表示をPDFへ反映

## v0.9.4
- PDF出力処理を互換性重視で再構築
- Canvasのbase64変換を廃止し、toBlob + ArrayBuffer方式へ変更
- PDF Blobを保存前にヘッダー/サイズ検証
- 直接ダウンロード非対応環境では新規タブ表示へフォールバック
- PDF失敗時に「どの処理段階で失敗したか」を表示
- Service Worker登録バージョンをv0.9.4へ更新し、旧PWAキャッシュ残留を抑制
- 天気予報の風速単位を km/h から m/s へ変更
- Open-Meteo取得時点から wind_speed_unit=ms を指定

## v0.9.5
- 年間予定PDFのみ生成処理を再設計
- 年間予定PDFのCanvasサイズを軽量化し、メモリ使用量を削減
- 1ページ生成ごとにCanvasメモリを解放
- canvas.toBlob失敗時はページ単位でdataURL方式へ自動フォールバック
- 通常モードで年間予定PDF生成に失敗した場合、さらに低解像度の軽量モードで自動再試行
- 大会スケジュールPDFはv0.9.4の正常動作方式を維持
- Service Workerをv0.9.5へ更新

## v0.9.6
- 天気・積雪のスキー場候補を「名前だけの候補」から「位置情報確認済み候補」へ変更
- 大会会場から都道府県/地域で候補を絞った後、各スキー場を事前ジオコーディング
- 位置情報を取得できたスキー場だけ候補一覧に表示
- 天気取得時は候補に保存済みの緯度・経度を直接使用し、再ジオコーディングを廃止
- スキー場名の正式名/略称/検索用別名を順番に試すフォールバックを追加
- Nominatimで取得できない場合はOpen-Meteo Geocoding APIへフォールバック
- 解決済み座標は端末LocalStorageへキャッシュし、次回以降の候補確認を高速化

## v0.9.7
- 常時表示するUIナビゲーションを「ホーム」「ツール切替」の2つだけに整理
- 年間予定、大会管理、大会会場、天気・積雪、選手管理、ランキング、チーム設定はホーム画面のショートカットからアクセス
- 各機能画面の内容・データ・機能は削除せず維持
- Service Workerをv0.9.7へ更新

## v0.9.8
- 常時表示ボタンを HOME / ツール切替 の2個に統一
- 2ボタンを同サイズ・横並びで配置
- 「ホーム」表記を「HOME」に変更
- 8個のホームメニュー上部にあった「ホーム」見出しを削除
- 年間予定PDFの生成失敗原因を修正
  - PDF描画処理が未定義の `HOLIDAYS` を参照していた
  - 正しい祝日Set `JP_HOLIDAYS` を参照するよう修正
- 年間予定PDFの対象月生成チェックを追加
- Service Workerをv0.9.8へ更新

## v0.9.9
- HOMEボタンの動作を修正
- どの機能ページに滞在中でもHOMEを押すとホーム画面へ戻る
- 現在表示中のsectionを解除し、#homeをactiveにする専用goHome()を追加
- HOMEボタンのactive状態も復元
- ホーム表示時にrenderHome()を再実行して直近1週間予定などを更新
- 画面上部へスクロールして戻る
- Service Workerをv0.9.9へ更新

## v0.10.0
- 上部の「ツール切替」ボタンを削除
- 上部常時表示ボタンは「HOME」のみ
- ホーム画面内8ボタンの「ツール切替」は維持
- HOMEボタンの戻る動作はv0.9.9を維持
- Service Workerをv0.10.0へ更新

## v0.10.1
- HOME「直近1週間の予定」の月日・曜日を曜日/祝日で色分け
- 日曜・祝日：赤
- 土曜：青
- 土曜日が祝日の場合：祝日を優先して赤
- 平日：従来色
- Service Workerをv0.10.1へ更新

## v0.10.2
- チーム名をHOMEボタンの右横へ表示
- チーム名はHOMEより大きめの太字フォントで表示
- PC/スマートフォンでバランスが崩れないレスポンシブサイズ
- チーム設定で名称変更後も上部表示へ反映
- Service Workerをv0.10.2へ更新

## v0.10.3
- ヘッダー内のチーム名表示を削除
- チーム名はHOMEボタン右横の表示だけを維持
- Service Workerをv0.10.3へ更新

## v0.10.4
- ヘッダーに残っていた旧チーム名表示 `teamHeader` を完全削除
- チーム名はHOMEボタン右横のみ表示
- HOME「直近1週間の予定」の期間表示（例：9/12 ～ 9/18）を削除
- 各日付・曜日と予定一覧はそのまま維持
- Service Workerをv0.10.4へ更新

## v0.10.5
- ヘッダーを2段構成に変更
- 下段に「Alpine Team Manager」を1行表示
- ヘッダーの「by TechBit」を削除
- 選手管理の「ポイント更新」「ポイントリスト参照」ボタンを削除
- 登録済み選手のポイント情報はアプリ起動時に自動更新
- 「競技者番号から登録」は見出しとして残し、入力欄と選手登録ボタンを常時表示
- その下に緑色の「ポイントリストから選択」見出しを追加
- ポイントリスト登録は「加盟団体 → 性別 → 一覧取得」の順へ変更
- 一覧取得後に選択画面を表示する既存動作を維持
- Service Workerをv0.10.5へ更新

## v0.10.6
- HOME「大会会場」を「会場情報」へ変更
- HOME「チーム設定」を「選手・チーム設定」へ変更
- 「選手・チーム設定」選択画面を新設
  - 選手管理
  - チーム設定
- HOMEの旧「選手管理」枠を「レース速報」へ変更
- 「レース速報」からSEIKO SPORTSLINKのアルペンスキー画面を別タブで開く
- 既存の選手管理・チーム設定画面自体は維持
- Service Workerをv0.10.6へ更新

## v0.10.7
- HOMEメニューを「管理」「情報」の2カテゴリに再編
- 管理：年間予定 / 大会管理 / チームランキング / 選手・チーム設定
- 情報：会場情報 / 天気・積雪 / レース速報 / ツール切り替え
- カテゴリごとに色味・見出し・説明を分け、視認性を改善
- スマートフォンでは2列を維持
- Service Workerをv0.10.7へ更新

## v0.10.8
年間予定カレンダー基本仕様を確定・実装。
- シーズン期間：5月〜翌4月
- 通常表示：11月〜翌4月
- オフシーズンON：5月〜翌4月
- 1日のカレンダー表示は最大2件
- 予定種類：大会 / 練習 / 合宿 / 移動 / 休養
- 種類別カラー表示
  - 大会：赤
  - 練習：青
  - 合宿：緑
  - 移動：オレンジ
  - 休養：グレー
- 日付セルをタップすると「予定種類＋20文字メモ」を2件まで直接編集
- 大会管理から登録した大会は年間予定へ自動反映し、直接編集画面では参照表示
- 3件以上データが存在する日はカレンダー上で2件＋残件数を表示
- PDFは従来仕様を維持（冬季2ページ / 全期間4ページ）し、予定種類を色分け
- Service Workerをv0.10.8へ更新

## v0.10.9
- HOMEと「操作ナビ」を同じ大きさで横並び配置
- チーム名を2ボタンの下段中央に表示
- 操作ナビ画面を追加
- 操作ナビではチーム設定 / 選手登録 / コーチ登録 / 大会登録 / 年間予定の状態を自動判定
- 「次におすすめの操作」と設定進捗を表示
- 各項目から既存の設定画面へ直接移動可能
- Service Workerをv0.10.9へ更新

## v0.11.0
- 年間予定の直接編集に「開始日」「終了日」を追加
- 例：3/1〜3/3を指定すると、3/1・3/2・3/3の各セルへ同じ予定内容を反映
- 期間は1日だけでも設定可能
- 期間予定にはrangeId / rangeStart / rangeEndを保存
- 期間内の任意セルを再編集すると元の期間を復元
- 期間変更時は旧期間を削除して新しい期間へ再反映
- 既存の大会管理データは上書きせず自動反映を維持
- 予定種類＋20文字メモ×2件の仕様を維持
- Service Workerをv0.11.0へ更新

## v0.11.1
- 期間予定の途中日をタップした際に編集対象を選択可能
  - この日だけ追記・編集
  - 期間全体を編集
- 例：3/1〜3/3の期間予定を残したまま、3/2だけ別予定を追記可能
- この日だけ編集では元の期間予定を他の日に維持
- 期間全体を編集では従来どおり開始日・終了日・内容を一括変更
- 1日最大2件表示の仕様を維持
- Service Workerをv0.11.1へ更新

## v0.11.2
- チームランキングの選手名をクリック可能に変更
- ランキングの選手名から選手詳細画面へ移動
- ランキング経由で開いた選手詳細では「← チームランキングへ戻る」を表示
- 選手管理経由で開いた場合は従来どおり「← 所属選手一覧へ戻る」
- 詳細画面の遷移元を保持して適切な画面へ戻る
- Service Workerをv0.11.2へ更新

## v0.11.3
- 選手詳細「過去大会成績」のスマートフォン表示をカード形式へ変更
- PCは従来の6列表を維持
- スマホは横スクロール不要
- カード表示順：日付 / 種目 / 大会名 / 順位 / タイム / 獲得P
- 種目はバッジ表示、順位と獲得Pを強調
- Service Workerをv0.11.3へ更新

## v0.11.4
- HOMEの「レース速報」を選択ページ方式へ変更
- レース速報ページに2つの選択肢を追加
  - SEIKO SPORTSLINK
  - THA&KS ALGE Timing
- どちらも別画面（新しいタブ）で開く
- ALGE Timing URL: http://116.58.169.162/alge/
- Service Workerをv0.11.4へ更新

## v0.11.5
- スマートフォンの時計・電波強度・バッテリー表示とヘッダーが重ならないよう上部余白を拡大
- iPhone/PWAのsafe-area-inset-topに対応
- スマホでは通常余白に加えてステータスバー領域分を自動確保
- PC表示への影響は最小限
- Service Workerをv0.11.5へ更新

## v0.11.6
- 選手詳細「過去大会成績」からタイム欄を削除
- PC表：日付 / 大会 / 種目 / 順位 / 獲得P の5列へ変更
- スマホカード：順位 / 獲得P の2項目表示へ整理
- Service Workerをv0.11.6へ更新

## v0.12.0
- アプリ全体のシーズン管理を導入
- 操作ナビのSTEP 1で最初に使用シーズンを選択
- 選択シーズンをdb.team.globalSeasonYearへ保存
- チーム名の横に全体シーズン選択欄を配置
  例：〇〇ジュニアチーム　2026/2027シーズン
- 年間予定 / SAJ大会管理 / 会場情報 / 天気・積雪の基準シーズンを連動
- 年間予定画面のシーズン変更も全体シーズンへ反映
- SAJ大会管理のシーズン変更も全体シーズンへ反映
- 旧scheduleSeasonYearは互換性のため全体シーズンと同期
- 年間予定の「表示シーズンをロック」は廃止
- 操作ナビ進捗を6項目（シーズンを含む）へ拡張
- Service Workerをv0.12.0へ更新

## v0.12.1
- チーム名横の全体シーズン選択欄を右寄せ
- シーズン選択肢の表示を簡略化
  - 2026/2027
  - 2027/2028
  のように年だけ表示
- 「シーズン」「昨シーズン／今シーズン／来シーズン」の付加表示を選択肢から削除
- Service Workerをv0.12.1へ更新

## v0.12.2
- ALGE Timingの起動方式を変更
- JavaScriptのwindow.open()を廃止
- 通常のHTTP直接リンク（a href）で開く方式へ変更
- Chrome/SafariでURLを直接開く場合に近い挙動へ変更
- SEIKO SPORTSLINKは従来方式を維持
- Service Workerをv0.12.2へ更新

## v0.12.3
- アプリ再起動後にチームランキングが消える問題を修正
- 起動時SAJ自動更新で、取得レスポンスにポイント値が欠けている場合は既存のSL/GS/SGポイントを保持
- 空データ・一時的な取得不足で保存済みポイントをnull/undefinedへ上書きしない
- 成績履歴も取得結果が空の場合は既存データを保持
- 成功した選手更新は即時LocalStorageへ保存
- ランキングは数値文字列も正しく扱うよう強化
- Service Workerをv0.12.3へ更新

## v0.12.4 Security / reliability audit
- Stored-XSS対策：選手・コーチ・大会・SAJ取得データの動的HTML出力をエスケープ
- SAJ大会詳細URLは `https://sajdb.shikuminet.jp` のみ許可
- バックアップJSON読込を5MB上限・危険なprototypeキー除外・基本構造検証へ変更
- CSP（Content-Security-Policy）と `Referrer-Policy: no-referrer` を追加
- Service Workerは同一オリジンの静的リソースだけをキャッシュ
- 外部APIレスポンスや選手情報をCache Storageへ保存しない
- Service Workerのキャッシュ削除を `snowtech-alpine-*` のみに限定
  （同一GitHub Pagesオリジンの他アプリのキャッシュを削除しない）
- Cloudflare WorkerはGET/OPTIONS以外を405で拒否
- Workerのdebug APIはENABLE_DEBUG=1の場合のみ有効
- Worker CORSをGitHub Pages公開元 `https://m6jm4s654p-lab.github.io` に制限
- Workerへ nosniff / no-referrer / DENY / Permissions-Policy を追加
- エラー応答は `Cache-Control: no-store`
- バックエンド変更を含むため `npx wrangler deploy` が必要

## v0.12.5 Local-only representative-coach review
運用前提：
- 1チームにつき代表コーチ1名が1端末で管理
- チーム間共有・クラウドDB・ログイン機能は使用しない
- SAJ公開データ取得のみCloudflare Workerを利用

追加確認・修正：
- 起動時にPWAキャッシュを全削除する処理を廃止
  - オフライン時にアプリ本体キャッシュが失われるリスクを解消
  - 古いキャッシュ削除はService Workerのバージョン更新時だけ実施
- LocalStorage保存失敗を検知し、保存エラーを表示
- バックアップ実施日時を端末内に記録
- 30日以上バックアップしていない場合に警告表示
- バックアップファイル名を日付付きへ変更
- バックアップJSONに選手情報が含まれる注意書きを追加
- 「全データ削除」を2段階確認へ変更
- 全削除時は本アプリ専用のLocalStorage / sessionStorage / Cacheだけを削除
- SAJ選手情報API（/api/saj-athlete, /api/saj-athletes）は `Cache-Control: no-store`
  - Cloudflare/ブラウザ側に選手取得レスポンスをキャッシュしない
- 大会など個人情報を含まない公開情報は5分キャッシュを維持

結論：
- 代表コーチ1名・端末内管理という前提では、クラウドDB/Auth/RLSは不要
- 最大の運用リスクは端末故障・ブラウザデータ消去なので、定期バックアップを重視
- WorkerはSAJ公開情報の中継のみで、チームDBは保持しない

## v0.12.6
- ALGE Timingの起動方式を再変更
- `target="_blank"` を廃止
- JavaScriptも使わず、通常の同一画面リンク `href="http://116.58.169.162/alge/"` で直接移動
- iPhoneホーム画面PWA / standaloneモードで、新規ウインドウからHTTPへ遷移できないケースを回避
- SEIKO SPORTSLINKは従来どおり別画面で開く
- ALGEを開いた後はブラウザ/端末の戻る操作でアプリへ戻る
- Service Workerをv0.12.6へ更新

## v0.12.7 ALGE iPhone PWA fix
- iPhoneのホーム画面PWAからHTTPサイトへ直接遷移できない問題への対策
- ALGE TimingをCloudflare Worker経由のHTTPSリバースプロキシで表示
- アプリ側リンク:
  https://snowtech-saj-api.take6583.workers.dev/alge/
- Worker側の接続先は固定:
  http://116.58.169.162/alge/
- 任意URLを指定できない固定プロキシとして実装し、open proxy / SSRF化を防止
- HTML内のALGE絶対URL・一部root-relative URLもHTTPSプロキシへ書き換え
- ALGEレスポンスはキャッシュしない
- この変更はworker.jsを含むためCloudflare Workerの再デプロイが必要

## v0.12.8 ALGE external-browser launch
- Cloudflare WorkerのALGE HTTPSプロキシ方式を撤回
- ALGEの実URL `http://116.58.169.162/alge/` を外部ブラウザで開く方式へ変更
- iPhone / iPad:
  - Chromeが入っている場合 `googlechrome://116.58.169.162/alge/` でChrome起動
  - Chromeが起動しない場合はALGE URLをコピーし、Safari/Chromeへ貼り付ける案内を表示
- Android:
  - `intent://...;scheme=http;package=com.android.chrome;end` でChrome起動
- PC/その他:
  - 通常のHTTP URLを別タブで開く
- iOSのSafariは、ホーム画面PWAから任意のHTTP URLをSafariへ強制的に渡す公開スキームが安定して使えないため、Chrome起動＋URLコピーを安全なフォールバックとする
- ALGEアクセスはCloudflare Workerを経由しない
- Service Workerをv0.12.8へ更新

## v0.12.9 usability / consistency fixes
- チームランキングに「最新ポイント基準（選択中シーズンには影響されません）」を明記
- 大会管理の登録済み一覧をアプリ全体で選択中のシーズンだけに限定
- 大会スケジュールPDFも選択中シーズンだけを出力
- PDF見出しに対象シーズンを明記
- 期間予定の「この日だけ編集」をslot単位のoverride方式へ整理
- 期間予定2件を編集画面で常に予定1 / 予定2として正しく表示
- 個別日で削除したslotはoverrideDeleteとして保持し、元期間を壊さない
- 期間全体を再編集した場合、その期間に紐づく個別日overrideを自動整理
- 旧バージョンの個別上書きデータも可能な範囲で継続認識
- SAJ大会12か月取得を4並列のバッチ取得へ変更
- 起動時選手ポイント更新を最大3並列へ変更（毎起動更新の仕様は維持）
- バックアップ表示を「最終バックアップ書き出し操作」とし、実保存完了を保証する表現を避ける
- Service Workerをv0.12.9へ更新
